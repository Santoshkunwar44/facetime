import {styled} from "styled-components"

export const  DashboardWrapper = styled.div`

padding: 2remfa;
width:100%;
.dashboardContainer{
    display: flex;
    gap: 2rem;
    align-items: flex-start;
}

`